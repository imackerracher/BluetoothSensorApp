package com.example.groupfourtwo.bluetoothsensorapp.visualization;

import static com.example.groupfourtwo.bluetoothsensorapp.data.Measure.PRESSURE;

/**
 * If the pressure icon is selected in the homescreen, this class will be called
 */

public class PressureActivity extends VisualizationActivity {

    public PressureActivity() {
        super(PRESSURE);
    }
}
